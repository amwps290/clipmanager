use clipmanager::*;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_basic_functionality() {
        // 基本的集成测试
        // 这里可以添加更多的集成测试
        assert!(true);
    }

    #[test]
    fn test_config_loading() {
        // 测试配置加载
        // 由于这是一个 GUI 应用，我们主要测试非 GUI 组件
        assert!(true);
    }
}
